# AI Marketing Suite — Python Dependencies
# Core (no external dependencies needed — scripts use stdlib)
# Optional (for enhanced functionality):
reportlab>=4.0        # PDF report generation
perplexityai>=1.0     # Perplexity Search API + Sonar API SDK
